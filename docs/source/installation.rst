Installation
============


Clone the repository::

   git clone https://github.com/BrysonGray/neurotrack.git

Change the directory to neurotrack and install the requirements::
   
   pip install -r requirements.txt

For details on the command line interface, see :doc:`usage`.